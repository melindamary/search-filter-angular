import { MyerrorDirective } from './myerror.directive';

describe('MyerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new MyerrorDirective();
    expect(directive).toBeTruthy();
  });
});
